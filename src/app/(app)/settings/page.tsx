import { ModuleStatus } from "@/components/shared/module-status";

export default function SettingsPage() {
  return (
    <ModuleStatus
      title="Settings"
      description="Household preferences — placeholder for V0.1."
      countLabel="Settings"
      count={0}
    />
  );
}
