/** Days ahead for warranty, maintenance, cancellation attention */
export const ATTENTION_WINDOW_DAYS = 30;

/** Max attention cards on Today dashboard */
export const MAX_ATTENTION_ITEMS = 5;

/** Max upcoming timeline entries */
export const MAX_UPCOMING_ITEMS = 12;
